# Repository Provenance

This standalone repository was extracted from the `tools/font-previewer` subtree of:

```text
repository: bomkino/pitch-deck-tools
branch:     codex/native-macos-font-lab
commit:     be77221cb7cb809fdf119945f3fee3d2e1e72ed6
```

The extraction preserves commits that changed the Font Previewer subtree while promoting that subtree to repository root.

```text
source commit:    be77221cb7cb809fdf119945f3fee3d2e1e72ed6
extracted commit: f89cde8c3e6e347e29829e5bcd7ee59160d7b3ad
```

The source repository and reference branch were not moved, rewritten, or deleted. Future work should cite the source commit when comparing behavior, fixtures, CI, or render output.

Root-level repository files that were outside the original subtree, including the MIT licence and Git ignore rules, were restored deliberately for the standalone repository.
